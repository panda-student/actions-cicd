# 通用Docker Compose配置
services:
  app:
    build:
      context: ..
      dockerfile: deployment/Dockerfile
    image: actions-cicd:${IMAGE_TAG:-latest}
    restart: unless-stopped
    ports:
      - "80:80"
